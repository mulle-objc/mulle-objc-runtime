#
# Used to be hardcoded in mulle-sde plugin, now part of the environment.
#
export PATH="${DEPENDENCY_DIR:-${MULLE_VIRTUAL_ROOT}/dependency}/Debug/bin:${DEPENDENCY_DIR:-${MULLE_VIRTUAL_ROOT}/dependency}/Release/bin:${DEPENDENCY_DIR:-${MULLE_VIRTUAL_ROOT}/dependency}/bin:${PATH}"


